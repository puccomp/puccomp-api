meta {
  name: Definir permissões do membro
  type: http
  seq: 5
}

put {
  url: {{base_url}}/v1/members/{{member_id}}/permissions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "permissions": ["roles:read"]
  }
}
