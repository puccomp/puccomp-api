meta {
  name: Ler as permissões do cargo
  type: http
  seq: 4
}

get {
  url: {{base_url}}/v1/authz/roles/{{role_id}}/permissions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
