meta {
    name: Criar curso
    type: http
    seq: 3
}

post {
    url: {{base_url}}/v1/courses
    body: json
    auth: bearer
}

auth:bearer {
    token: {{access_token}}
}

body:json {
    {
        "name": "Engenharia Elétrica"
    }
}
