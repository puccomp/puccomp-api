meta {
  name: Listar PATs
  type: http
  seq: 2
}

get {
  url: {{base_url}}/v1/auth/pat
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

script:post-response {
  if (res.getStatus() === 200 && res.getBody().length > 0)
    bru.setEnvVar("pat_id", res.getBody()[0].id);
}
