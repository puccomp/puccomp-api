meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{owner_email}}",      // member_email
    "password": "{{owner_password}}" // member_password
  }
}

script:post-response {
  if (res.getStatus() === 200) 
    bru.setEnvVar("access_token", res.getBody().access_token);
}
