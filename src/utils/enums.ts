enum TechnologyUsageLevel {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  SUPPORTIVE = 'supportive',
  OBSOLETE = 'obsolete',
}

enum TechnologyType {
  LANGUAGE = 'language',
  FRAMEWORK = 'framework',
  LIBRARY = 'library',
  TOOL = 'tool',
  OTHER = 'other',
}

export { TechnologyUsageLevel, TechnologyType }
